{% extends 'base.html.twig' %}

{% block title %}{{ title }}{% endblock %}

{% block body %}
    <div class="container flex flex-col justify-center items-center">
        {{ form_start(form) }}
            {{ form_widget(form) }}
            <button type="submit" class="mt-3 px-4 py-2 font-semibold text-sm bg-cyan-500 text-white rounded-full shadow-sm">Enregistrer</button>
        {{ form_end(form) }}
        <a href="{{ path('project_list') }}" class="mt-3 px-4 py-2 font-semibold text-sm bg-cyan-500 text-white rounded-full shadow-sm">Retour à la liste des projets</a>
    </div>
{% endblock %}
